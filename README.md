# Alone-scanig-
Free Tool Good Tool Total free 
install cmd

dpkg --configure -a && apt --fix-broken install -y && pkg update -y && pkg upgrade -y && pkg install python git -y && git clone https://github.com/maheshhhhe62-source/Alone-scanig-.git && cd Alone-scanig- && pip install requests httpx rich dnspython h2 && python -u *.py

cd ~/Alone-scanig-

python -u *.py

Enjoy totally free tool and good no any stuck txt file scan and CIDR scaner working good by @Alonee_op [ Telegram  ]
